set imap_user = "bhaisahab@baagi.org:993"
#set imap_pass = ""
set smtp_url = "smtp://bhaisahab@baagi.org:25"
#set smtp_pass = ""

set spoolfile = "imaps://baagi.org"
set imap_idle = yes

mailboxes = +INBOX

set hostname = baagi.org
set realname = 'bhaisahab'
set from="bhaisahab@baagi.org"
set use_from = yes

set copy = yes
set mbox_type = Maildir
set folder = $HOME/mail
set record = +sent
set mbox = +mbox
set postponed = +postponed

set mail_check = 30
set timeout = 300
set imap_keepalive = 500
set imap_check_subscribed
set sort=threads

set ssl_starttls=yes
set ssl_force_tls=yes

set header_cache="~/.mutt/cache/headers"
set message_cachedir="~/.mutt/cache/bodies"
set certificate_file=~/.mutt/certificates

alternative_order text/plain text/html *
auto_view text/html

source ~$MAILCONF/.muttrc-colors
